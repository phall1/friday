{
  "name": "friday",
  "private": true,
  "description": "Friday local dictation app build toolchain.",
  "scripts": {
    "postinstall": "patch-package",
    "check": "native check",
    "test": "tsx --test tests/*.test.ts && zig build test",
    "build": "zig build",
    "package": "bash scripts/package-macos.sh"
  },
  "dependencies": {
    "@native-sdk/core": "0.10.1"
  },
  "devDependencies": {
    "@native-sdk/cli": "0.10.1",
    "patch-package": "8.0.0",
    "tsx": "^4.20.6"
  }
}
